# 日志系统说明

## 已实现功能

- 日志级别：`DEBUG`、`INFO`、`WARN`、`ERROR`
- 同步日志：业务线程直接写入日志文件
- 异步日志：业务线程写入有界阻塞队列，日志线程负责落盘
- 日志切分：日期变化时创建新文件；达到最大行数时创建序号文件
- 安全退出：关闭队列后，日志线程会写完队列中的剩余日志再退出

日志格式：

```text
2026-06-12 12:30:45.123 [INFO] [tid 12345] Server running on port 8080
```

日志文件名示例：

```text
server_2026-06-12.log
server_2026-06-12.1.log
```

## 配置

配置通过环境变量传入：

| 环境变量 | 默认值 | 说明 |
| --- | --- | --- |
| `LOG_ENABLED` | `1` | `1` 启用日志，`0` 关闭日志 |
| `LOG_ASYNC` | `0` | `1` 异步，`0` 同步 |
| `LOG_LEVEL` | `INFO` | 最低输出级别 |
| `LOG_FILE` | `server.log` | 日志基础路径 |
| `LOG_QUEUE_SIZE` | `8192` | 异步阻塞队列容量 |
| `LOG_MAX_LINES` | `50000` | 单个日志文件最大行数 |

同步启动示例：

```bash
LOG_ENABLED=1 LOG_ASYNC=0 LOG_LEVEL=INFO ./webserver
```

异步启动示例：

```bash
LOG_ENABLED=1 LOG_ASYNC=1 LOG_LEVEL=DEBUG \
LOG_FILE=logs/server.log LOG_QUEUE_SIZE=8192 \
LOG_MAX_LINES=50000 ./webserver
```

## 验证

```bash
make test-logging
```

测试覆盖异步写入、阻塞队列、小容量队列、级别过滤、按行切分和关闭日志。
